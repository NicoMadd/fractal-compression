/**
 * Block transforms for fractal domain–range matching.
 * <p>
 * Convention: {@code matrix[i][j]} is row {@code i}, column {@code j}. The gray codec
 * passes square {@code n×n} blocks; rotations and {@code REFLECTION_SD} use one side
 * length {@code n} (typically {@code rows == cols}).
 */
package implementations.java.compression.src.utils.fractal.transformation;
